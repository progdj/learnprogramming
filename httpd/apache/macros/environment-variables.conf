<Macro environment-variables $licenseholderId>

    <If "'${ENVIRONMENT}' == ''">
        SetEnv ENVIRONMENT development
    </If>

    Define ENVIRONMENT_LICENSEHOLDER ${ENVIRONMENT}-$licenseholderId

</Macro>